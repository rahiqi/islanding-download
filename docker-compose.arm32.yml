# Dispatcher is 64-bit only; this override is not used for the main stack.
# For agent on Raspberry Pi 32-bit use: docker compose -f agent/docker-compose.yml -f agent/docker-compose.arm32.yml up -d

services: {}
